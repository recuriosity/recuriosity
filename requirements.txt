# -----------------------------------------------------------------------
# Curious Camera — Python dependencies
#
# Install order matters for source-built packages (gsplat, habitat-sim):
#   1. pip install torch torchvision torchaudio  (see below)
#   2. pip install fused-ssim (no-build-isolation, before gsplat)
#   3. pip install -r requirements.txt           (includes gsplat build)
#   4. Install habitat-sim from source (see README)
#
# PyTorch is NOT listed here so you can choose the right CUDA wheel for
# your driver. Install it first:
#   pip install torch==2.4.1 torchvision==0.19.1 torchaudio==2.4.1 \
#       --extra-index-url https://download.pytorch.org/whl/cu124
# -----------------------------------------------------------------------

xformers==0.0.28

einops==0.8.1
gymnasium==0.29.1
imageio==2.37.2
imageio-ffmpeg==0.6.0
matplotlib==3.10.1
# numpy>=2.0 required by habitat-sim; cap below 2.3 for compatibility
numpy>=2.0,<2.3.0
opencv-python-headless==4.12.0.88
Pillow==10.0.1
PyYAML==6.0.3
scikit-image==0.23.2
scikit-learn==1.7.1
scipy==1.15.3
tensorboard==2.20.0
timm==1.0.21
trimesh>=4.0.0
tqdm==4.67.1
viser==1.0.16
wandb==0.21.2
nerfview==0.1.3
splines

# gsplat — built from source (requires torch to be installed first)
gsplat @ git+https://github.com/nerfstudio-project/gsplat.git@b60e917c95afc449c5be33a634f1f457e116ff5e
